// Re-export Supabase client and configuration
export { supabase, supabaseConfig } from '../config/supabase';

// Re-export authentication middleware
export { authMiddleware } from '../middleware/auth';
